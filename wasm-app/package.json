{
  "name": "template",
  "version": "0.1.0",
  "description": "run app in the browser",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "build": "vite build",
    "wasm-pack-dev": "wasm-pack build ../example --out-dir ../wasm-app/pkg --dev",
    "wasm-pack-release": "wasm-pack build ../example --out-dir ../wasm-app/pkg --release"
  },
  "devDependencies": {
    "runner": "file:pkg",
    "vite": "^6.2.2",
    "vite-plugin-wasm": "^3.4.1"
  }
}
